# Test and lint tooling. Not needed to run the app, and deliberately not in
# requirements.txt, which the launcher installs into every user's environment.
#
#   pip install -r requirements-dev.txt
#
# One linter, not three. black was here but never ran: CI dropped it because
# this codebase is not black-formatted, so leaving it installed only invited
# somebody to reformat five thousand lines by accident. ruff replaces flake8
# and reads its rules from pyproject.toml, so the local run and the CI run
# cannot drift apart.
-r requirements.txt
pytest==7.4.3
ruff==0.12.2
